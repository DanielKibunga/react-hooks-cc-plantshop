import './test_suites/AllPlants.test'
import './test_suites/CreatePlant.test'
import './test_suites/InStock.test'
import './test_suites/SearchPlants.test'
// import { render, fireEvent, act } from '@testing-library/react'; // Ensure you import act

// test('adds a new plant when the form is submitted', async () => {
//   const { getByText } = render(<App />);
  
//   // Wrap the action causing state updates in act()
//   await act(async () => {
//     fireEvent.click(getByText('Add Plant'));
//   });
  
//   // Your assertions here
// });
